import { useLang } from "@/lib/i18n";
import { ArrowLeft, MessageCircle, Mail } from "lucide-react";
import { Link } from "react-router-dom";
// Ne felejts el egy jó képet importálni magadról!
// import hostPhoto from "@/assets/host-portrait.jpg"; 

const HostProfile = () => {
  const lang = useLang();

  return (
    <div className="min-h-screen bg-white text-black font-body">
      {/* Vissza gomb */}
      <nav className="fixed top-0 w-full p-6 z-50">
        <Link to="/" className="flex items-center gap-2 text-[10px] tracking-[0.3em] uppercase hover:gap-4 transition-all">
          <ArrowLeft size={14} /> {lang === "hu" ? "Vissza" : "Back"}
        </Link>
      </nav>

      <div className="container mx-auto px-6 pt-32 pb-24 max-w-5xl">
        <div className="grid grid-cols-1 md:grid-cols-2 gap-16 items-start">
          
          {/* Kép szekció */}
          <div className="relative aspect-[3/4] bg-gray-100 overflow-hidden">
             {/* <img src={hostPhoto} className="w-full h-full object-cover" alt="The Host" /> */}
             <div className="absolute inset-0 flex items-center justify-center text-gray-400 italic">
               [ Helyezz ide egy minőségi portrét ]
             </div>
          </div>

          {/* Szöveg szekció */}
          <div className="space-y-12">
            <header>
              <p className="text-[10px] tracking-[0.5em] uppercase text-gray-400 mb-2">Házigazda</p>
              <h1 className="font-display text-5xl font-light tracking-tighter">Szemetics László</h1>
            </header>

            <div className="space-y-6 text-gray-600 leading-relaxed font-light">
              <p>
                {lang === "hu" 
                  ? "Az Aura nem csupán egy vendégház számomra, hanem egy régi álom megvalósulása Révfülöp szívében. Célom egy olyan tér létrehozása volt, ahol a modern építészet és a Balaton-felvidék nyugalma találkozik."
                  : "For me, Aura is not just a guesthouse, but the realization of a long-held dream in the heart of Révfülöp. My goal was to create a space where modern architecture meets the serenity of the Balaton Uplands."}
              </p>
              <p>
                {lang === "hu"
                  ? "Hiszek a részletek erejében, a reggeli kávé illatában a teraszon és a csendben, amit csak a Balaton tud nyújtani."
                  : "I believe in the power of details, the aroma of morning coffee on the terrace, and the silence that only Lake Balaton can provide."}
              </p>
            </div>

            {/* Kapcsolati pontok */}
            <div className="pt-8 border-t border-gray-100 space-y-4">
              <p className="text-[10px] tracking-[0.2em] uppercase font-bold text-black">Foglalás közvetlenül:</p>
              <div className="flex flex-col gap-4">
                <a href="https://wa.me/TELEFONSZAMOD" className="flex items-center gap-4 group">
                  <div className="p-3 border border-black rounded-full group-hover:bg-black group-hover:text-white transition-all">
                    <MessageCircle size={18} />
                  </div>
                  <span className="text-sm tracking-widest uppercase">WhatsApp</span>
                </a>
                <a href="mailto:email@cimed.com" className="flex items-center gap-4 group">
                  <div className="p-3 border border-black rounded-full group-hover:bg-black group-hover:text-white transition-all">
                    <Mail size={18} />
                  </div>
                  <span className="text-sm tracking-widest uppercase">Email</span>
                </a>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default HostProfile;
